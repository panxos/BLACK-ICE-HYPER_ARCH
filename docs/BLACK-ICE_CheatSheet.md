# 🧊 BLACK-ICE ARCH - Keyboard Shortcuts Cheat Sheet

![Header](cheatsheet_header_cyberpunk_1770097644673.png)

## 🖥️ Hyprland (Window Management)

| Keybinding | Action |
| :--- | :--- |
| **`SUPER` + `Return`** | Launch Terminal (**Kitty**) |
| **`SUPER` + `Q`** | Kill Active Window |
| **`SUPER` + `D`** | App Launcher (**Wofi**) |
| **`SUPER` + `E`** | Text Editor (**Kate**) |
| **`SUPER` + `F`** | Fullscreen |
| **`SUPER` + `Space`** | Maximize (Keep Bar) |
| **`SUPER` + `V`** | Toggle Floating |
| **`SUPER` + `J`** | Toggle Split |
| **`SUPER` + `L`** | Lock Screen (**Hyprlock**) |
| **`SUPER` + `M`** | Exit Hyprland |
| **`SUPER` + `ALT` + `W`** | Wallpaper Switcher |
| **`SUPER` + `ALT` + `Y`** | Theme Selector |

### 🚀 Application Shortcuts (Win + Shift + Key)

| Keybinding | Application |
| :--- | :--- |
| **`Win` + `Shift` + `A`** | Antigravity AI |
| **`Win` + `Shift` + `B`** | Brave Browser |
| **`Win` + `Shift` + `F`** | Firefox |
| **`Win` + `Shift` + `D`** | Dolphin (File Manager) |
| **`Win` + `Shift` + `K`** | KeePassXC |
| **`Win` + `Shift` + `O`** | Obsidian |
| **`Win` + `Shift` + `C`** | Caido |

---

## 📸 Screenshots

| Keybinding | Action |
| :--- | :--- |
| **`Print`** | Select area & edit (Swappy) |
| **`SHIFT` + `Print`** | Fullscreen to Clipboard |
| **`SUPER` + `Print`** | Select area & save to file |
| **`CTRL` + `Print`** | Fullscreen & save to file |

---

## 🐚 Zsh Aliases & Productivity

| Alias | Description |
| :--- | :--- |
| **`update-system`** | Full System Update (Pacman + Paru) |
| **`IPS`** | Show Net Interfaces & IPs in a table |
| **`ll` / `la`** | Improved Directory Listing (`lsd`) |
| **`cat`** | Improved Content Viewing (`bat`) |
| **`orphans`** | Cleanup unused packages |
| **`ytb`** | Watch YouTube in Terminal (`mpv`) |
| **`start-git`** | Start SSH agent & check GitHub conn |

---

## 🛡️ Cybersecurity Tools & Functions

| Function / Alias | Description |
| :--- | :--- |
| **`cvpn` / `kvpn`** | Check or Kill VPN connections |
| **`mkt`** | Create Pentesting dir structure |
| **`extractPorts`** | Extract open ports from Nmap gnmap file |
| **`pingsweep`** | ICMP sweep on a subnet (e.g. `pingsweep 192.168.1.0/24`) |
| **`find_suid`** | Search for SUID binaries (PrivEsc) |
| **`nclisten`** | Start Netcat listener on port (default 4444) |
| **`pyrev`** | Generate Python Reverse Shell |
| **`b64shell`** | Generate Base64 encoded reverse shell |
| **`webserver`** | Quick Python HTTP Server |

---

> [!TIP]
> **Pro Tip**: Use `SUPER` + `1-5` to switch workspaces fast! 🧊
