#!/bin/bash
# wallpaper_visual: Selector visual de wallpapers con thumbnails (waypaper)
# Fallback a selector de texto si waypaper no está instalado
# BLACK-ICE ARCH | P4nx0z

# Detectar directorio de wallpapers
if command -v xdg-user-dir &>/dev/null; then
    PICTURES_DIR=$(xdg-user-dir PICTURES)
    [ -z "$PICTURES_DIR" ] && PICTURES_DIR="$HOME/Pictures"
else
    PICTURES_DIR="$HOME/Pictures"
fi

WALL_DIR="$PICTURES_DIR/wallpapers"

# Preferir waypaper (thumbnails reales)
if command -v waypaper &>/dev/null; then
    waypaper --folder "$WALL_DIR" --backend awww
    exit 0
fi

# Fallback: selector wofi con texto (sin thumbnails)
CACHE_FILE="$HOME/.cache/current_wallpaper"

if [ ! -d "$WALL_DIR" ]; then
    notify-send "Wallpaper" "No se encontró $WALL_DIR" --urgency=critical
    exit 1
fi

mapfile -t walls < <(find "$WALL_DIR" -type f \
    \( -iname "*.png" -o -iname "*.jpg" -o -iname "*.jpeg" \
       -o -iname "*.webp" \) 2>/dev/null | sort)

if [ ${#walls[@]} -eq 0 ]; then
    notify-send "Wallpaper" "No hay wallpapers en $WALL_DIR" --urgency=critical
    exit 1
fi

CURRENT=$(cat "$CACHE_FILE" 2>/dev/null)
mapfile -t display_list < <(
    for w in "${walls[@]}"; do
        rel="${w#$WALL_DIR/}"
        [ "$w" = "$CURRENT" ] && echo "★ $rel" || echo "  $rel"
    done
)

SELECTION=$(printf '%s\n' "${display_list[@]}" | \
    wofi --dmenu --prompt "󰋩  Wallpaper:" \
         --width 500 --height 400 --cache-file /dev/null --insensitive --no-actions)

[ -z "$SELECTION" ] && exit 0

rel_path="${SELECTION#★ }"; rel_path="${rel_path#  }"
FULL_PATH="$WALL_DIR/$rel_path"
[ ! -f "$FULL_PATH" ] && { notify-send "Wallpaper" "No encontrado: $rel_path" --urgency=critical; exit 1; }

echo "$FULL_PATH" > "$CACHE_FILE"
pkill mpvpaper 2>/dev/null
pgrep -x awww-daemon > /dev/null || { awww-daemon &; sleep 0.5; }
awww img "$FULL_PATH" --transition-type grow --transition-duration 1
notify-send "BLACK-ICE" "Fondo: $(basename "$FULL_PATH")" --urgency=low
